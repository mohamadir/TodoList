# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Blink' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Blink

pod 'PopupDialog', '~> 1.1'
pod 'SwiftyJSON', '~> 4.0'

pod 'Alamofire', '~> 5.2'
  target 'BlinkTests' do
    inherit! :search_paths
    # Pods for testing
  end

 
  target 'BlinkUITests' do
    # Pods for testing
  end

end
